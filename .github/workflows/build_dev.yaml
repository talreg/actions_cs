name: build-dev
on:
  push:
    branches:
      - main
jobs:    
  getvars: 
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: running build
        uses: ./.github/workflows/build.yaml
        with:
          environment: ${{vars.SERVER_LOCATION}}

