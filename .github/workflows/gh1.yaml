name: gh1
on: 
  workflow_dispatch:
jobs:
  gh1:
    runs-on: ubuntu-latest
    steps:
    - name: github output
      run: echo " ${{toJson( github)}}"
    - name: runner output
      run: echo " ${{toJson( runner)}}"
            